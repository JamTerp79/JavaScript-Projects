function myFunction()  { // The Function //
    var sentence = "I am learning";// Creating a sentence variable //
    sentence += " a lot from this book!";// Concatenating the strings //
    document.getElementById("Concatenate").innerHTML = sentence;// Runs the function when id is called in html //
}